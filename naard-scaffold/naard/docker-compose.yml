services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: naard
      POSTGRES_PASSWORD: naard_dev
      POSTGRES_DB: naard
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
